# Setup

Fedora:
```bash
sudo dnf install cmake g++ glfw-devel
```

Debian:
```bash
sudo apt install cmake g++ libglfw3-dev libxi-dev
```

# Execution
```bash
mkdir build
cd build
cmake .. && make
```
